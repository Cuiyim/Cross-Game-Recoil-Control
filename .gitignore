# ── 构建与发布产物（可自动重建，勿提交）──
bin/
obj/
publish/

# ── 本地 .NET 缓存 ──
.dotnet-cli/
.dotnet-home/

# ── IDE / 编辑器 ──
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# ── 运行期 / 调试产物 ──
*.pdb
*.log
*.tmp
image-diagnostic.png
Profiles/

# ── 程序在 exe 旁生成的本地配置 ──
LegendaryCSharp.settings.json
LegendaryCSharp.image-recognition.json

# ── 发行二进制走 GitHub Releases，不进源码 ──
*.exe
*.zip

# ── AI 助手 / Cowork 交互产物（保留在本地，仅不提交）──
.claude/
.cowork/
*.skill
*-workspace/
feedback.json
eval_set*.json
.__*.tmp
